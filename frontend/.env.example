# frontend/.env.example
# Keep the browser API path relative so the same build works with the local Vite proxy
# and with the production Nginx reverse proxy.
VITE_API_BASE_URL=/api/v1
VITE_APP_TITLE="AI Incident Response Platform"
VITE_ENABLE_ANALYTICS=false
